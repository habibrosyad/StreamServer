package edu.monash.streaming.streamserver;

public enum StatisticType {
    JOIN,
    JOIN_TIME,
    PROBE,
    REMOVE,
    OUTPUT,
    WINDOWING,
    WINDOWING_TIME,
    MISS,
    INPUT,
    EXECUTION_TIME
}
