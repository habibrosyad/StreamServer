package edu.monash.streaming.streamserver;

public enum StatisticType {
    JOIN,
    JOIN_TIME,
    JOIN_TIME_MIN,
    JOIN_TIME_MAX,
    PROBE,
    REMOVE,
    OUTPUT,
    WINDOWING,
    WINDOWING_TIME,
    WINDOWING_TIME_MIN,
    WINDOWING_TIME_MAX,
    MISS,
    INPUT,
    EXECUTION_TIME
}
